# Quotation Generator

## Run locally

```bash
npm install
npm run dev
```

Open the local URL shown by Vite.

## Build for deployment

```bash
npm run build
```

The production files are generated in the `dist/` folder. Preview that build locally with `npm run preview`.
